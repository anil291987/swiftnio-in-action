import XCTest
@testable import swiftnio_in_action

final class swiftnio_in_actionTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(swiftnio_in_action().text, "Hello, World!")
    }


    static var allTests = [
        ("testExample", testExample),
    ]
}
